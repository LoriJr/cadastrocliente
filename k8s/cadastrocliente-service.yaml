apiVersion: v1
kind: Service
metadata:
  name: cadastrocliente
  namespace: default
  annotations:
    cloud.google.com/backend-config: '{"default": "app-backend-config"}'
spec:
  selector:
    app: cadastrocliente
  ports:
    - port: 80
      targetPort: 8081
      protocol: TCP
  type: NodePort
#  type: LoadBalancer
